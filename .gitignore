# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Config (contains local IP / port settings — copy redrive_config.json.example instead)
redrive_config.json

# touch_assets/ — tracked fully (add anatomy PNGs and tool cursor PNGs freely)
# Runtime per-room uploads are NOT committed — they're regenerated from user sessions
touch_assets/anatomy/_uploads/

# PyInstaller / Inno Setup build output
dist/
build/
*.spec.bak

# OS
.DS_Store
Thumbs.db
desktop.ini
